SpecFlow extension. Download Winium.Desktop.Driver.exe and keep it ready. Start the Winium driver before running the tests: Run Winium.Desktop.Driver.exe to start the automation server. Build the project in Visual Studio to restore dependencies.

Notes Ensure the Clock App is accessible on your system. Replace hardcoded paths (e.g., Winium Driver path) with your local paths in the code. used UiSpy.exe to find elements
